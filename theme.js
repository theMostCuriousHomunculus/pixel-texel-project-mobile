import { DefaultTheme } from 'react-native-paper';

const theme = {
  ...DefaultTheme,
  colors: {
    ...DefaultTheme.colors,
    green: '#4da05b',
    blue: '#509bc8',
    red: '#db1c1c',
    gray: '#d3d3d3'
  }
};

export default theme;